server:
  port: ${SERVER_PORT:8081}

spring:
  application:
    name: queueless-ai
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/queueless_ai?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:Password}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: false
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:mohammedsadiq4850@gmail.com}
    password: ${MAIL_PASSWORD:bvzg udpj bvxl ncuj}
    properties:
      mail:
        smtp:
          auth: ${MAIL_SMTP_AUTH:true}
          starttls:
            enable: ${MAIL_STARTTLS_ENABLE:true}

springdoc:
  swagger-ui:
    path: /swagger-ui.html

app:
  frontend:
    url: ${FRONTEND_URL:http://localhost:5555}
  jwt:
    secret: ${JWT_SECRET:YOUR_SUPER_SECURE_JWT_SECRET_KEY_THAT_IS_LONG_ENOUGH_FOR_HS256}
    expiration-ms: ${JWT_EXPIRATION_MS:86400000}
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5009,http://127.0.0.1:5009,http://localhost:5555,http://127.0.0.1:5555}
  seed:
    enabled: ${SEED_DEMO_DATA:true}
  notifications:
    email-enabled: ${EMAIL_NOTIFICATIONS_ENABLED:true}
    from: ${MAIL_FROM:QueueLess AI <mohammedsadiq4850@gmail.com>}
  queue:
    default-service-minutes: ${DEFAULT_SERVICE_MINUTES:8}

razorpay:
  key:
    id: ${RAZORPAY_KEY_ID:rzp_test_T6J8RMHePPRV5h}
    secret: ${RAZORPAY_KEY_SECRET:H15jaKlC0CZeH16nmGDt6i62}
